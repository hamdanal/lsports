from __future__ import annotations

from setuptools import setup

raise SystemExit("Unsupported installation method, use `pip install .` instead.")

# Only to make GitHub detect the python packaging.
setup(
    name="lsports",
    requires=[],
)
